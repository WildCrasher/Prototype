<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.marcin.prototype">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CreateActivity" />
        <activity android:name=".TrackDetailsActivity" />
        <activity android:name=".TrackViewActivity" />
        <activity android:name=".EndOfTrackActivity" />
        <activity android:name=".PremiumActivity" />
        <activity android:name=".DestinationActivity" />
        <activity android:name=".TracksListActivity" />
        <activity android:name=".TrackDetailsFirstAvtivity"></activity>
    </application>

</manifest>